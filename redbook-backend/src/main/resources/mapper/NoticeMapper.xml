<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhiyan.redbookbackend.mapper.NoticeMapper">

    <select id="listInteractions" resultType="com.zhiyan.redbookbackend.dto.resp.InteractionRow">
        <![CDATA[
        SELECT type, actor_id AS actorId, note_id AS noteId, event_time AS eventTime FROM (
          SELECT 'LIKE' AS type, nl.user_id AS actor_id, nl.note_id AS note_id, nl.create_time AS event_time
          FROM tb_note_like nl INNER JOIN tb_note n ON n.id = nl.note_id
          WHERE n.user_id = #{ownerId} AND nl.user_id <> n.user_id
          UNION ALL
          SELECT 'COLLECT' AS type, nc.user_id AS actor_id, nc.note_id AS note_id, nc.create_time AS event_time
          FROM tb_note_collect nc INNER JOIN tb_note n ON n.id = nc.note_id
          WHERE n.user_id = #{ownerId} AND nc.user_id <> n.user_id
          UNION ALL
          SELECT 'COMMENT' AS type, c.user_id AS actor_id, c.note_id AS note_id, c.create_time AS event_time
          FROM tb_note_comment c INNER JOIN tb_note n ON n.id = c.note_id
          WHERE n.user_id = #{ownerId} AND c.user_id <> n.user_id AND c.status = 0
          UNION ALL
          SELECT 'SHARE' AS type, sl.creator_user_id AS actor_id, sl.note_id AS note_id, sl.create_time AS event_time
          FROM tb_share_link sl INNER JOIN tb_note n ON n.id = sl.note_id
          WHERE n.user_id = #{ownerId} AND sl.creator_user_id <> n.user_id
        ) t
        ORDER BY event_time DESC
        LIMIT #{offset}, #{limit}
        ]]>
    </select>

    <select id="countInteractions" resultType="java.lang.Long">
        <![CDATA[
        SELECT COUNT(1) FROM (
          SELECT nl.id FROM tb_note_like nl INNER JOIN tb_note n ON n.id = nl.note_id
          WHERE n.user_id = #{ownerId} AND nl.user_id <> n.user_id
          UNION ALL
          SELECT nc.id FROM tb_note_collect nc INNER JOIN tb_note n ON n.id = nc.note_id
          WHERE n.user_id = #{ownerId} AND nc.user_id <> n.user_id
          UNION ALL
          SELECT c.id FROM tb_note_comment c INNER JOIN tb_note n ON n.id = c.note_id
          WHERE n.user_id = #{ownerId} AND c.user_id <> n.user_id AND c.status = 0
          UNION ALL
          SELECT sl.short_code FROM tb_share_link sl INNER JOIN tb_note n ON n.id = sl.note_id
          WHERE n.user_id = #{ownerId} AND sl.creator_user_id <> n.user_id
        ) x
        ]]>
    </select>

    <select id="listLikeCollect" resultType="com.zhiyan.redbookbackend.dto.resp.InteractionRow">
        <![CDATA[
        SELECT type, actor_id AS actorId, note_id AS noteId, event_time AS eventTime FROM (
          SELECT 'LIKE' AS type, nl.user_id AS actor_id, nl.note_id AS note_id, nl.create_time AS event_time
          FROM tb_note_like nl INNER JOIN tb_note n ON n.id = nl.note_id
          WHERE n.user_id = #{ownerId} AND nl.user_id <> n.user_id
          UNION ALL
          SELECT 'COLLECT' AS type, nc.user_id AS actor_id, nc.note_id AS note_id, nc.create_time AS event_time
          FROM tb_note_collect nc INNER JOIN tb_note n ON n.id = nc.note_id
          WHERE n.user_id = #{ownerId} AND nc.user_id <> n.user_id
        ) t
        ORDER BY event_time DESC
        LIMIT #{offset}, #{limit}
        ]]>
    </select>

    <select id="countLikeCollect" resultType="java.lang.Long">
        <![CDATA[
        SELECT COUNT(1) FROM (
          SELECT nl.id FROM tb_note_like nl INNER JOIN tb_note n ON n.id = nl.note_id
          WHERE n.user_id = #{ownerId} AND nl.user_id <> n.user_id
          UNION ALL
          SELECT nc.id FROM tb_note_collect nc INNER JOIN tb_note n ON n.id = nc.note_id
          WHERE n.user_id = #{ownerId} AND nc.user_id <> n.user_id
        ) x
        ]]>
    </select>

    <select id="listCommentShare" resultType="com.zhiyan.redbookbackend.dto.resp.InteractionRow">
        <![CDATA[
        SELECT type, actor_id AS actorId, note_id AS noteId, event_time AS eventTime FROM (
          SELECT 'COMMENT' AS type, c.user_id AS actor_id, c.note_id AS note_id, c.create_time AS event_time
          FROM tb_note_comment c INNER JOIN tb_note n ON n.id = c.note_id
          WHERE n.user_id = #{ownerId} AND c.user_id <> n.user_id AND c.status = 0
          UNION ALL
          SELECT 'SHARE' AS type, sl.creator_user_id AS actor_id, sl.note_id AS note_id, sl.create_time AS event_time
          FROM tb_share_link sl INNER JOIN tb_note n ON n.id = sl.note_id
          WHERE n.user_id = #{ownerId} AND sl.creator_user_id <> n.user_id
        ) t
        ORDER BY event_time DESC
        LIMIT #{offset}, #{limit}
        ]]>
    </select>

    <select id="countCommentShare" resultType="java.lang.Long">
        <![CDATA[
        SELECT COUNT(1) FROM (
          SELECT c.id FROM tb_note_comment c INNER JOIN tb_note n ON n.id = c.note_id
          WHERE n.user_id = #{ownerId} AND c.user_id <> n.user_id AND c.status = 0
          UNION ALL
          SELECT sl.short_code FROM tb_share_link sl INNER JOIN tb_note n ON n.id = sl.note_id
          WHERE n.user_id = #{ownerId} AND sl.creator_user_id <> n.user_id
        ) x
        ]]>
    </select>

    <select id="listFollowNotice" resultType="com.zhiyan.redbookbackend.dto.resp.InteractionRow">
        <![CDATA[
        SELECT 'FOLLOW' AS type, f.follower_id AS actorId, NULL AS noteId, f.create_time AS eventTime
        FROM tb_user_follow f
        WHERE f.followee_id = #{ownerId}
        ORDER BY f.create_time DESC
        LIMIT #{offset}, #{limit}
        ]]>
    </select>

    <select id="countFollowNotice" resultType="java.lang.Long">
        SELECT COUNT(1) FROM tb_user_follow WHERE followee_id = #{ownerId}
    </select>

    <select id="countUnreadLikeCollect" resultType="java.lang.Long">
        SELECT COUNT(1) FROM (
          SELECT nl.id FROM tb_note_like nl INNER JOIN tb_note n ON n.id = nl.note_id
          WHERE n.user_id = #{ownerId} AND nl.user_id &lt;&gt; n.user_id
          <if test="since != null">AND nl.create_time &gt; #{since}</if>
          UNION ALL
          SELECT nc.id FROM tb_note_collect nc INNER JOIN tb_note n ON n.id = nc.note_id
          WHERE n.user_id = #{ownerId} AND nc.user_id &lt;&gt; n.user_id
          <if test="since != null">AND nc.create_time &gt; #{since}</if>
        ) x
    </select>

    <select id="countUnreadFollowNotice" resultType="java.lang.Long">
        SELECT COUNT(1) FROM tb_user_follow f
        WHERE f.followee_id = #{ownerId}
        <if test="since != null">AND f.create_time &gt; #{since}</if>
    </select>

    <select id="countUnreadCommentShare" resultType="java.lang.Long">
        SELECT COUNT(1) FROM (
          SELECT c.id FROM tb_note_comment c INNER JOIN tb_note n ON n.id = c.note_id
          WHERE n.user_id = #{ownerId} AND c.user_id &lt;&gt; n.user_id AND c.status = 0
          <if test="since != null">AND c.create_time &gt; #{since}</if>
          UNION ALL
          SELECT sl.short_code FROM tb_share_link sl INNER JOIN tb_note n ON n.id = sl.note_id
          WHERE n.user_id = #{ownerId} AND sl.creator_user_id &lt;&gt; n.user_id
          <if test="since != null">AND sl.create_time &gt; #{since}</if>
        ) x
    </select>
</mapper>
